spring:
  application:
    name: NoteMicroservice
  cloud:
    config:
      uri:
      - http://localhost:8888
  profiles:
    active:
    - production
eureka:
  client:
    service-url: 
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8763                                       